# 프로젝트 방향성 및 계획

## 1. 최종 목표

AI 도구의 도움을 받아, **프로젝트마다 코드 구조를 시각적으로 설명해주는 앱**을 만든다. 완성된 결과물은 앱 형태이면서 동시에 **영상 콘텐츠**로도 표현한다.

이를 위해 먼저 두 축을 학습한다.

1. 프로젝트 구조를 어떻게 이해할 것인가 (아키텍처 패턴, 코드 분석 방법론)
2. 그 구조를 어떻게 효과적으로 시각화할 것인가 (다이어그램 설계, 정보 시각화 원칙)

## 2. 지금까지 진행한 것

### 2-1. 학습 소재로 RhythmTrainer 프로젝트 선정
C4 폴더에 있던 실제 iOS 프로젝트(iOS 폰 앱 + watchOS 워치 앱)를 시각화 연습 대상으로 삼음. Views / ViewModels / Models / Services / WatchConnectivity 등으로 나뉜 실제 코드 구조를 확인함.

### 2-2. 시각화 도구 방향 정리
- 정적 구조 다이어그램: Mermaid
- 실제 의존성 그래프 자동 추출: Python(AST 파싱) + Graphviz
- 인터랙티브 탐색(발표·영상용): HTML/D3.js 위젯

### 2-3. 코드 내부 구조 패턴 학습
MVC → MVP → MVVM 순으로 개념과 차이를 정리하고, RhythmTrainer의 실제 코드에 각 구성 요소(View/ViewModel/Model/Service)를 대응시킴. RhythmTrainer는 순수 MVVM이 아니라 Service 계층이 추가된 4계층 구조임을 확인.
→ 산출물: `UI_아키텍처_패턴_정리.md`

### 2-4. 아키텍처 시각화 방법론 학습
C4 모델(Context → Container → Component → Code)의 4단계 줌 레벨 개념을 정리하고, RhythmTrainer에 각 레벨을 매핑함. "보는 사람의 역할에 따라 필요한 확대 수준이 다르다"는 핵심 원칙 확인.
→ 산출물: `C4_모델_정리.md`

## 3. 두 학습이 앱 설계에 주는 시사점

| 학습 내용 | 앱에 적용할 아이디어 |
|---|---|
| MVVM 등 아키텍처 패턴 | 코드를 스캔해 자동으로 "이 파일이 View인지 ViewModel인지 Model인지" 분류하는 기능의 근거가 됨 |
| C4 모델 | 앱의 화면 자체를 줌 레벨(전체 맥락 → 컨테이너 → 컴포넌트 → 코드)로 설계하면, 발표용 요약부터 개발자용 상세까지 하나의 앱으로 커버 가능 |
| 정적 분석 필요성 | Component/Code 레벨은 자동 파싱, Context/Container 레벨은 사람이 직접 정의하는 하이브리드 구조가 현실적 |

## 4. 앞으로의 계획 (단계별)

### 1단계 — 개념 학습 마무리 (진행 중)
- [x] MVC / MVP / MVVM 정리
- [x] C4 모델 정리
- [ ] 정적 분석(AST 파싱) 기초 학습
- [ ] 정보 시각화 원칙(레이아웃, 색상 인코딩, 인터랙션 설계) 학습
- [ ] Clean Architecture, MVI 등 추가 패턴 확장 학습(선택)

### 2단계 — 도구 프로토타입 제작
- RhythmTrainer 코드를 대상으로 실제 의존성 그래프를 자동 추출하는 스크립트 작성 (Python + AST + Graphviz)
- Mermaid로 수동 아키텍처 다이어그램 작성해 자동 추출 결과와 비교
- C4 모델의 4단계 중 최소 Container/Component 레벨을 시제품으로 시각화

### 3단계 — 인터랙티브 앱 형태로 통합
- 폴더 트리 + 클릭 시 파일 역할/관계를 보여주는 HTML 기반 뷰어 제작
- 줌 인/아웃으로 C4 레벨을 전환하는 UX 적용
- 다른 프로젝트(rhythmpractice 등)에도 같은 파이프라인 적용해 범용성 검증

### 4단계 — 영상 콘텐츠화
- 완성된 시각화 결과를 바탕으로 "이 프로젝트는 이렇게 구성되어 있다"를 설명하는 영상 제작
- Context → Container → Component → Code 순서로 스토리텔링 구성 (C4 모델의 확대 순서를 그대로 영상 내레이션 구조로 사용)

### 5단계 — 다른 프로젝트로 확장
- RhythmTrainer 외 다른 프로젝트에도 동일 도구를 적용해 "프로젝트마다" 라는 목표 완성
- 언어/프레임워크가 다른 프로젝트(예: 웹, 백엔드)에도 적용 가능한지 검증

## 5. 현재 산출물 목록

- `UI_아키텍처_패턴_정리.md` — MVC/MVP/MVVM 개념 및 RhythmTrainer 적용
- `C4_모델_정리.md` — C4 모델 4단계 개념 및 RhythmTrainer 적용
- `프로젝트_방향성_및_계획.md` — 본 문서
